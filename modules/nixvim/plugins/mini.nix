{
  plugins.mini = {
    enable = true;
    modules = {
      ai = { };
      comment = { };
      move = { };
      operators = { };
      pairs = { };
      splitjoin = { };
      surround = { };
      bracketed = { };
      jump = { };
    };
  };
}
